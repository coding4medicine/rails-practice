<%= form_for(buy_book) do |f| %>
  <% if buy_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(buy_book.errors.count, "error") %> prohibited this buy_book from being saved:</h2>

      <ul>
      <% buy_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
